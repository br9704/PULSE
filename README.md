# Pulse

Real-time campus occupancy for university students.

## Tech Stack
React 18 + TypeScript · Vite PWA · Tailwind CSS · Mapbox GL JS · Supabase (Realtime + Edge Functions + Postgres) · Google Places API · Recharts · Framer Motion · Turf.js

## Local Setup

### Prerequisites
- Node.js 18+
- pnpm (`npm install -g pnpm`)
- A Supabase project (free tier sufficient)
- A Mapbox account (free tier sufficient)
- A Google Cloud project with Places API enabled

### Steps

1. Clone and install
   ```bash
   git clone <repo-url>
   cd pulse
   pnpm install
   ```

2. Create `.env.local` from the example
   ```bash
   cp .env.example .env.local
   ```

3. Fill in environment variables:
   - `VITE_SUPABASE_URL` — from Supabase project settings
   - `VITE_SUPABASE_ANON_KEY` — from Supabase project settings
   - `VITE_MAPBOX_TOKEN` — from Mapbox account tokens page

4. Set Edge Function secrets in Supabase dashboard (not in .env):
   - `GOOGLE_PLACES_API_KEY`
   - `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_EMAIL`

5. Run locally
   ```bash
   pnpm dev
   ```

6. Run database migrations
   ```bash
   # Requires Supabase CLI
   supabase db push
   ```

## Key Documents
- `PRD.md` — Product requirements, feature specs, data models
- `CLAUDE.md` — Agent operating contract, coding standards, sprint tracking protocol
- `MASTERPLAN.md` — Sprint-by-sprint implementation plan (generated by Claude Code)

## Project Structure
```
src/
  components/   Reusable UI components
  hooks/        Custom React hooks (data fetching, realtime, geo)
  lib/          Supabase client, Mapbox helpers, utilities
  pages/        Top-level route components
  types/        Shared TypeScript interfaces
  stores/       Zustand state stores
  constants/    App constants (colours, thresholds, defaults)
supabase/
  migrations/   SQL migrations (numbered sequentially)
  functions/    Edge Functions (Deno)
  seed/         Seed data scripts
```

## Development Process
All work follows the sprints defined in `MASTERPLAN.md`. Claude Code marks each task complete before moving on. The PRD and MASTERPLAN are the sources of truth — see `CLAUDE.md` for full agent instructions.
